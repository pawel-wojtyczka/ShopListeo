import "../styles/global.css";
import { ViewTransitions } from "astro:transitions";

interface Props {
  title?: string;
}

const { title = "ShopListeo" } = Astro.props;

<!doctype html>
<html lang="pl" class="h-full">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/png" href="/favicon.png" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <ViewTransitions />
  </head>
  <body class="h-full bg-background text-foreground antialiased">
    <div class="flex h-full min-h-screen flex-col md:flex-row">
      <!-- Nawigacja boczna -->
      <div class="w-64 border-r hidden md:block p-4">
        <div class="text-xl font-bold mb-6">ShopListeo</div>
        <nav class="space-y-2">
          <a href="/" class="flex items-center p-2 rounded-lg hover:bg-muted text-foreground">
            Listy zakupów
          </a>
        </nav>
      </div>
      
      <!-- Główny obszar treści -->
      <main class="flex-1 overflow-auto">
        <slot />
      </main>
    </div>
  </body>
</html> 