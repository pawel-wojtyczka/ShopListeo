---
import LoginView from "@/components/auth/LoginView";
import AuthLayout from "@/layouts/AuthLayout.astro";

// export const prerender = false; // Usunięto - strona będzie generowana statycznie

// Logika przekierowania, jeśli użytkownik jest już zalogowany,
// powinna być obsłużona po stronie klienta w LoginView lub AuthContext.
// if (Astro.locals.isAuthenticated) { // Usunięto logikę serwerową
// return Astro.redirect("/");
// }
---

<AuthLayout title="Logowanie">
  <LoginView client:load />
</AuthLayout>
