---
import MainLayout from "@/layouts/MainLayout.astro";
import ShoppingListsView from "@/components/ShoppingListsView";
// import type { ShoppingListSummaryDTO, PaginationResponse } from "@/types"; // Już niepotrzebne tutaj
// import { getAllShoppingLists } from "@/lib/services/shopping-list.service"; // Już niepotrzebne tutaj
// import { logger } from "@/lib/logger"; // Już niepotrzebne tutaj

// export const prerender = false; // Usunięte, aby umożliwić statyczne generowanie

// Logika pobierania danych po stronie serwera została usunięta
// Komponent kliencki ShoppingListsView będzie teraz odpowiedzialny za pobieranie danych

// const { user, supabase } = Astro.locals; // To nadal może być potrzebne, jeśli MainLayout tego wymaga lub dla innych celów po stronie klienta, ale nie do pobierania danych tutaj
---

<MainLayout title="Twoje Listy Zakupów">
  {/* Komponent kliencki sam pobierze dane */}
  <ShoppingListsView client:load />
  {/* fetchError nie jest już przekazywany, komponent obsłuży błędy po stronie klienta */}
</MainLayout>
